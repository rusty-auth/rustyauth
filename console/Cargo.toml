[package]
name = "rustyauth-console"
version = "0.1.0"
edition = "2024"
rust-version = "1.94.1"
license = "Apache-2.0"
description = "Cross-platform RustyAuth fleet management console"
publish = false

[dependencies]
base64 = "=0.22.1"
buffa = { version = "=0.8.1", features = ["json"] }
connectrpc = { version = "=0.8.1", default-features = false }
dioxus = { version = "=0.7.10", default-features = false, features = ["launch", "lib"] }
dx_icons_tabler = "=0.1.0"
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
uuid = { version = "=1.24.0", features = ["js", "v4"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "=0.2.127"
wasm-bindgen-futures = "=0.4.77"
js-sys = "=0.3.104"
serde-wasm-bindgen = "=0.6.5"
web-sys = { version = "=0.3.104", features = [
    "Credential",
    "CredentialsContainer",
    "Headers",
    "History",
    "Location",
    "Navigator",
    "PublicKeyCredential",
    "Request",
    "RequestCredentials",
    "RequestInit",
    "RequestMode",
    "Response",
    "Window",
] }

[build-dependencies]
connectrpc-build = "=0.8.0"
protoc-bin-vendored = "=3.2.0"

[features]
default = ["web"]
web = ["dioxus/web"]
desktop = ["dioxus/desktop"]
mobile = ["dioxus/mobile"]

[profile.release]
codegen-units = 1
lto = "thin"
strip = "symbols"
