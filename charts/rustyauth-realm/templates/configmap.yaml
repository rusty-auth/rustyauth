{{- if not .Values.config.existingConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rustyauth-realm.configMapName" . }}
  labels:
    {{- include "rustyauth-realm.labels" . | nindent 4 }}
data:
  {{ .Values.config.existingConfigMapKey }}: |
    {{- include "rustyauth-realm.config" . | nindent 4 }}
{{- end }}
