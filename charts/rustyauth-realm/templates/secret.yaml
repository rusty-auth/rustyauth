{{- if and .Values.secrets.create .Values.secrets.existingSecret }}{{ fail "secrets.create and secrets.existingSecret are mutually exclusive" }}{{- end }}
{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rustyauth-realm.secretName" . }}
  labels:
    {{- include "rustyauth-realm.labels" . | nindent 4 }}
type: Opaque
stringData:
  AUTH_MASTER_KEY_HEX: {{ required "secrets.values.masterKeyHex is required when secrets.create=true" .Values.secrets.values.masterKeyHex | quote }}
  BOOTSTRAP_TOKEN: {{ required "secrets.values.bootstrapToken is required when secrets.create=true" .Values.secrets.values.bootstrapToken | quote }}
  AUTH_EVENT_RPC_TOKEN: {{ required "secrets.values.eventRpcToken is required when secrets.create=true" .Values.secrets.values.eventRpcToken | quote }}
  AUTH_IDENTITY_RPC_TOKEN: {{ required "secrets.values.identityRpcToken is required when secrets.create=true" .Values.secrets.values.identityRpcToken | quote }}
  {{- range $key, $value := .Values.secrets.extra }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
